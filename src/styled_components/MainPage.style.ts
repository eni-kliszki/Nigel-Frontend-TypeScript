import styled from "styled-components";


//We should find a better way to do this, this should only be temporary solution
export const MainPageContainer = styled.div`
    position: absolute;
    top: 90px;
`